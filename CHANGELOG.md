# Changelog

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은 첫 공개 배포부터 붙인다.

## [Unreleased]

### 추가

- **커밋 가드를 켜는 `setup.sh`.** `.githooks/pre-commit` 은 커밋돼 있지만 `core.hooksPath` 가 `.git/config` 에 있어 클론과 함께 오지 않는다. 그래서 새로 클론한 곳과 워크트리에서는 가드가 조용히 꺼져 있었다. `setup.sh` 가 그 설정을 걸고, 실행 비트를 채우고, 되읽어 확인한 뒤 어떤 패턴 출처가 잡히는지 알린다. 개인 패턴은 저장소별 `.private/guard-patterns` 외에 저장소 밖의 공용 목록(`$GIT_GUARD_PATTERNS`, 없으면 `~/.config/git-guard/patterns`)에서도 읽으므로, 저장소가 늘어도 목록은 하나다. `bash` 와 `git` 만 있으면 된다.
- **저장소 프로필이 커밋 가드가 꺼진 저장소를 세션 머리에서 알린다.** 훅 폴더(`.githooks` 또는 `.husky`)가 있는데 `core.hooksPath` 가 없으면 꺼졌다고 알리고 켜는 명령을 함께 준다. 설정돼 있으면 그 값을 보인다. 훅 폴더가 없는 저장소에는 줄을 넣지 않는다. 가드를 쓰지 않는 곳까지 권유를 실으면 이 모듈의 원칙(사실만 싣고 행동 지시는 넣지 않는다)에서 벗어나기 때문이다.

### 수정

- **`setup.sh` 가 남이 잡은 `core.hooksPath` 를 덮어쓰지 않는다.** 이 설정은 값을 하나만 가져서, husky·lefthook 이 이미 잡고 있는 저장소에서는 그쪽 훅이 조용히 죽었다. 이제 기존 값이 다르면 멈추고, 공존하는 방법과 덮어쓰는 방법을 함께 알린다. 공존은 그쪽 pre-commit 에 한 줄을 넣는 것이고, 덮어쓰기는 `--force` 다. `--force` 를 기본값으로 두지 않은 이유는 남의 훅을 버리는 것이 사람이 정할 일이기 때문이다.

### 변경

- **README 를 다시 배치했다.** 설치 명령까지 약 2,600자를 읽어야 했던 순서를 바꿔, 리드 다음에 무엇이 막히는지와 설치가 오고 설득은 뒤로 갔다. 머리말은 가운데 정렬한 목차와 언어 줄로 바꿨다. 훅 열둘의 배선표는 게이트 상세 문서로 옮기고 README 에는 게이트 넷 요약만 남겼다. 두 번 나오던 인용 하나를 지우고, 메시지 언어 설정 단락을 "끄기와 제거" 로 옮겼다. 1.8.0 에서 넣은 "사람이 보지 않는 세션에서는 근거 게이트가 돌지 않는다" 를 "언제 도나" 에 적었다. 한국어와 영어 두 판을 같이 고쳤다.
- **"강제한다" 는 표현을 "확인한다" 로 바꿨다.** 첫 표의 항목도 "~하지 않습니다" 에서 "~합니다" 로 돌렸다. 영어판의 `recommend` 대 `enforces` 대비도 함께 바꿨다. 인용문과 남이 스스로 적은 설명문은 그대로 두었다.
- **이식 안내 문서(`docs/PUBLIC-REPO-GUARD.md`)를 없애고 셋으로 나눴다.** 다른 저장소에 가드를 적용하는 절차는 이 저장소의 제품과 무관하고, 같은 절차가 사용자 스킬에도 있어 두 곳이 갈라져 있었다. husky 를 쓰지 않은 이유는 설계 결정이므로 `docs/decisions.md` 로, 기여자가 알아야 할 것은 `CONTRIBUTING` 두 판의 본문으로 옮겼다.
- **CodeQL 이 워크플로 YAML 도 본다.** 지금까지 분석 대상이 `.py` 세 개뿐이라 실제 공격면인 GitHub Actions 는 아무도 보지 않았다. 언어 식별자 `actions` 를 매트릭스에 더해 고정되지 않은 액션, 과한 토큰 권한, 스크립트 인젝션을 본다. `codeql-action` 세 액션은 버전이 갈리면 죽으므로 dependabot 이 한 PR 로 묶도록 설정했다.

## [1.8.0] - 2026-09-16

### 추가

- **`/check:config` 가 게이트 언어를 전역에도 쓸 수 있다.** 언어를 고른 뒤 쓸 곳을 한 번 더 묻는다. 이 저장소를 고르면 지금처럼 `.check.toml` 의 `lang` 한 줄을, 전역을 고르면 `~/.claude/settings.json` 의 `env.NGG_LANG` 키 하나만 바꾼다. 전역 값은 모든 저장소의 `lang` 보다 앞서고 PR 에 남지 않으므로, 그 사실을 선택지 설명에 적게 했다.
- 전역 설정 파일에는 토큰이 들어 있을 수 있다. 그래서 스킬이 그 파일을 통째로 출력하지 않고 `env.NGG_LANG` 키만 보게 했다. 바꾼 뒤에도 게이트가 옛 언어로 말하면 새 세션을 열라고 안내한다.

### 제거

- **R4 를 뺐다.** 막힌 뒤 도구 없이 다시 끝내는 답을 막던 규칙이다. 출처로 적어 둔 Reduce hallucinations 는 뒷받침을 찾지 못한 주장을 철회하라고 하는데, R4 는 도구 없이 철회한 답까지 막았다. 다시 쓴 답은 나머지 규칙으로만 판정한다. `disabled_rules` 에 `R4` 를 적어 두었다면 이제 없는 이름으로 알린다.
- **`pg.noverify` 를 뺐다.** `git commit --no-verify` 를 막던 검사다. 막으라고 권하는 문서를 찾지 못했다. 프로젝트 가드는 이제 `append_only` 경로만 본다. 저장소 프로필의 "설정 없어 --no-verify만 차단"은 "설정 없어 막는 것 없음"이 됐다.
- **프로젝트 가드가 마이그레이션 삭제를 막지 않는다.** `append_only` 경로의 기존 파일을 `rm`·`git rm` 으로 지우는 명령을 막던 부분이다. 출처인 Best practices 는 "blocks writes to the migrations folder" 까지만 말하고, Rails 마이그레이션 가이드는 스키마 파일이 기준이 되면 오래된 마이그레이션을 지워도("delete or prune") 된다고 설명한다. 기존 파일 수정 차단만 남긴다. 차단 메시지가 "삭제나 이동을 막는다"고 했지만 이동은 원래 막지 못하고 있었다. 이제 훅은 Edit·Write 에만 걸려 Bash 호출마다 돌지 않는다.

### 변경

- **사람이 보지 않는 세션에서는 근거 게이트가 돌지 않는다.** 다른 플러그인이 `claude -p` 로 띄운 자동 요약 세션이 실제 차단의 대부분을 차지했는데(V48 에서 오탐 40턴), 그 세션의 답은 사람에게 보이지 않아 되물을 상대가 없다. `CLAUDE_CODE_SESSION_ATTENDED=0` 이거나 `CLAUDE_CODE_ENTRYPOINT=sdk-cli` 이면 건너뛰고 그 사실을 `events.log` 에 `exempt:headless` 로 남긴다. 두 변수 모두 공식 훅 문서에 없어 실측으로만 확인했으므로, 값이 없으면 지금처럼 막는다. 헤드리스로 돌면서 차단을 기대하는 회귀 테스트와 CI 는 `NGG_HEADLESS=1` 로 되살린다.
- **불가 면제가 알아보는 표현을 넓혔다.** `판단할 수 없`·`단정할 수 없`·`알 수 없`·`cannot determine`·`cannot tell` 을 더했다. 도구가 없어 답할 수 없다고 정직하게 밝혔는데도 막히던 오탐이다(V48 에서 17턴).
- **인용은 단정으로 보지 않는다.** 따옴표·백틱 안(80자 이내)의 내용은 R1 판정 전에도 지운다. 문서나 로그에서 파일 이름을 옮겨 적은 문장이 파일 상태를 단정한 것으로 읽히던 오탐이다. 다만 괄호 안의 내용은 R1 에서 지우지 않는다. `설정 파일(config.json)이 없습니다` 처럼 괄호에 파일 이름을 적은 단정까지 빠져나가기 때문이다.
- **R2b 는 도구를 쓰지 않은 턴에만 건다.** 근거 문장("Never speculate about code you have not opened")은 열어 보지 않은 코드에 대한 것이라, 도구로 파일을 연 턴의 추정 표현은 막지 않는다.
- **PR 본문 검사(`done.pr`)는 성공이나 검증을 주장하는 본문만 본다.** 근거 문장은 "Have Claude show evidence rather than asserting success" 라서, 변경만 설명하는 본문에는 코드 블록을 요구하지 않는다.
- **`it.todo(` 추가는 테스트 무력화로 보지 않는다.** 아직 쓰지 않은 테스트의 자리라 있던 테스트를 끄지 않는다. `.skip(`·`xit(` 같은 표기는 그대로 막는다.
- **R2a 는 코드베이스 맥락에서만 건다.** 근거 문장("BEFORE answering questions about the codebase")이 범위를 코드베이스 질문으로 한정한다. 프롬프트가 저장소·파일을 묻지 않고, 답에 경로나 "여기 있는 파일"(`scripts here`, `여기 있는 테스트 파일`) 같은 표현도 없으면 유보 표현이 있어도 막지 않는다. 처음 좁혔을 때는 답 속의 "scripts here"를 맥락으로 읽지 못해 selftest `tp-defer` 가 새어 나갔고, 그 표현을 더해 고쳤다.
- 근거 표의 출처를 원문과 다시 맞췄다. R0·R1·R2a·R2b 는 Prompting best practices 의 `investigate_before_answering`, R5 는 Best practices, `ti.assert`·`ti.exclude` 는 EvilGenie 의 "Modified Testing Procedure", 마이그레이션 예시는 Best practices 로 고쳤다. 확인일은 2026-09-15 다. `/check:config` 표도 같이 고쳤다.
- 막혔을 때 나가는 안내에서 R4 를 지우고, 뒷받침할 근거를 찾지 못한 단정이나 검증 주장은 철회하라고 적었다. 사용자가 실행을 막은 경우의 예시("사용자가 실행하지 말라고 해서 확인할 수 없다")도 더했다.

## [1.7.0] - 2026-09-14

### 바뀐 것 — 버전

- **2.0.0 을 철회하고 1.7.0 으로 다시 맞춘다.** 2.0.0 은 grounded → did-you-check 리네임을 major 로 올린 것인데, 커맨드와 설정 이름이 아직 굳지 않아 major 를 붙일 단계가 아니었다고 보고 1.x 로 되돌린다. 리네임 자체는 그대로 두고 버전 번호만 내린다.
- **v2.0.0 태그와 릴리스만 지운다.** 나머지 1.x 이력(v1.0.0 ~ v1.6.1)은 그대로 둔다. v2.0.0 이 남아 있으면 semver 정렬에서 최신으로 잡혀 마켓플레이스와 뱃지가 계속 2.0.0 을 가리키기 때문이다. 그 커밋은 main 에 그대로 있으므로 코드는 사라지지 않고, 필요하면 태그와 릴리스를 다시 만들 수 있다.
- 이력인 CHANGELOG 의 [2.0.0] 항목은 그대로 둔다. 그때 실제로 나갔던 릴리스의 기록이라, 번호를 내렸다고 지우면 무슨 일이 있었는지 남지 않는다.

### 변경

- 매니페스트 두 곳의 버전을 1.7.0 으로 맞췄다. `plugin/.claude-plugin/plugin.json` 의 `version` 과 `.claude-plugin/marketplace.json` 의 `plugins[0].version` 이다. 최상위 `marketplace.json` 의 `version`(마켓플레이스 자체 버전)은 건드리지 않는다.

## [2.0.0] - 2026-09-13

### 바뀐 것 — 이름

- **이름이 did-you-check 다.** grounded 는 LLM 을 다루는 사람에게만 뜻이 통했다. 맥락 없이 이름만 준 모델에게 무엇을 하는 플러그인일지 물었더니 예전 후보는 "추천 항목을 체크해 두는 할 일 목록" 으로 읽혔다. 검사를 강제한다는 뜻은 전해지지 않았다. 이름이 하는 일을 말하게 만든다.
- **사용자가 직접 치는 것이 넷 다 바뀐다.** 호환 장치는 두지 않는다.
  - 설치: `/plugin marketplace add IsthisLee/did-you-check` 다음 `/plugin install check@did-you-check`
  - 커맨드: `/check:init` 등 여덟
  - 설정 파일: `.grounded.toml` → `.check.toml`
  - 한 번 허용: 프롬프트에 `check allow <항목>`
  - 상태 폴더: `~/.claude/plugins/data/check-did-you-check/`
- **옛 `.grounded.toml` 은 읽지 않는다.** 그 파일만 남은 저장소는 `test_command`·`append_only`·`disabled_rules` 를 잃고 완료 게이트가 "검사 명령을 찾지 못했다" 로 지나간다. 파일 이름만 바꾸면 그대로 동작한다.
- 이력인 CHANGELOG 와 `docs/VERIFICATION.md` 의 옛 이름은 그대로 둔다. 그때 그 이름으로 잰 기록이라 고치면 기록이 아니게 된다.

### 변경

- **README 를 모범 사례 준수 검사 관점으로 다시 썼다.** 첫 표가 "이런 일이 생기면" 이 아니라 **검사하는 모범 사례**를 세로로 놓고 그 옆에 어긴 순간을 붙인다. 네 줄이 모두 공식 문서나 검증된 문서에서 왔다는 것과 어느 문장에서 왔는지를 README 안에서 바로 보이게 근거 표를 옮겨 왔다. 한국어·영어·설치본 셋 다 고쳤다.
- **설치본 README 의 상태 폴더 경로가 틀려 있었다.** `grounded-inline` 이라고 적었는데 그것은 `--plugin-dir` 로 띄웠을 때의 이름이다. 마켓플레이스로 설치하면 `<플러그인>-<마켓플레이스>` 다.

## [1.6.1] - 2026-09-11

### 수정
- **README가 낡아 있었다.** 검증 기록을 "V1부터 V16까지"라고 적었지만 기록은 V37까지 있다. 다시 낡지 않게 번호를 뺐다. "비슷한 도구" 절은 grounded가 턴 끝에서만 막는다고 소개했다. 실제로는 테스트 무력화·근거 없는 PR·커밋 전 검사를 도구 호출 단계에서 막는다. 끄기 표에 `/grounded:config`를 넣고 CI 문장을 Windows fuzz가 main에서만 도는 지금 동작에 맞췄다.
- **턴마다 붙는 비용을 다시 쟀다.** 326ms라고 적어 왔는데 같은 스크립트로 v1.5.0과 1.6.0을 번갈아 재니 258ms와 256ms였다. 훅별 차이는 5ms 안이다. 표에 빠져 있던 `done-gate/pre.sh`·`post.sh`를 넣고 도구마다 붙는 비용을 훅이 실제로 걸리는 도구 기준으로 고쳐 적었다(Bash 한 번 약 170ms, Edit·Write 한 번 약 155ms).
- **하위 폴더로 `cd`하면 완료 게이트가 조용히 통과했다.** 훅 입력의 `cwd`는 Claude가 `cd`하면 따라가는데 게이트들이 `.grounded.toml`을 cwd에서만 찾았다. `cd pkg/api` 뒤에는 검사 명령도 `disabled_rules`도 `append_only`도 못 찾아서 턴 끝 검사와 커밋 전 검사가 돌지 않고 지나갔다. 이제 cwd에서 위로 올라가며 `.grounded.toml`이나 `.git`이 있는 첫 폴더를 루트로 쓴다. 명령 안의 상대 경로는 그대로 cwd 기준으로 푼다.
- **서브에이전트가 고친 코드를 완료 게이트가 보지 못했다.** 편집 기록이 서브에이전트 폴더에 적혀 메인 턴 끝의 검사가 읽지 못했다. 이제 세션 폴더에 적는다.

### 변경
- **관계없는 Bash 명령에서는 훅이 파이썬을 띄우지 않는다.** 테스트 무결성·프로젝트 가드·완료 게이트의 `pre.sh`는 Bash 호출마다 돌며 매번 파이썬으로 입력을 읽었다. 이제 도구 이름을 파라미터 확장으로 먼저 보고 Bash인데 삭제·이동·커밋·PR 생성 글자가 없으면 바로 끝낸다. `ls -la`에서 세 훅이 42·64·41ms에서 12ms 안팎이 되어 Bash 한 번에 붙는 비용이 약 170ms에서 약 70ms로 줄었다. Edit·Write와 관련 명령은 전처럼 끝까지 검사한다.

## [1.6.0] - 2026-09-11

### 추가
- **PR 본문에 근거가 없으면 PR을 열지 못한다.** 완료 게이트가 `gh pr create` 직전에 본문을 보고, 닫힌 코드 블록도 이미지도 없으면 막는다. 공식 문서의 "Have Claude show evidence rather than asserting success"를 PR에 적용했다. 지금까지는 `/grounded:ship`이 권하기만 해서 커맨드를 거치지 않은 PR에는 아무것도 걸리지 않았다. 문서만 바꾼 브랜치와 본문을 명령에서 볼 수 없는 경우(`--fill`·`--web`)는 막지 않는다. **형식만 본다.** 붙인 출력이 진짜인지는 가리지 못한다.
- **게이트의 검사 하나만 끄는 길.** `disabled_rules`가 근거 게이트의 R0~R5에 더해 다른 게이트의 항목 이름도 받는다. `done.turn`·`done.commit`·`done.pr`, `ti.skip`·`ti.assert`·`ti.rm`·`ti.exclude`, `pg.noverify`다. 지금까지는 PR 본문 검사 하나를 끄려 해도 `NGG_DONE=0`으로 턴 끝 검사까지 같이 꺼야 했고, 그 환경변수는 팀에 보이지 않았다. 끈 사실은 `events.log`에 `off=[...]`로 남고, 없는 이름은 어느 게이트든 막을 때 알린다.
- **오탐 한 건만 넘기는 길.** 다음 프롬프트에 `grounded allow ti.skip`처럼 한 줄을 쓰면 그 턴에 해당 검사가 한 번 통과하고 허용은 사라진다. 설정으로 끄면 계속 꺼져 있어서, 픽스처 문자열 하나 때문에 막힌 편집을 넘기려 해도 검사를 통째로 꺼야 했다. 허용은 사용자 프롬프트에서만 읽으므로 모델이 스스로 풀 수 없다. 근거 게이트의 R0~R5는 대상이 아니다.
- **`/grounded:config`.** 게이트 항목마다 무엇을 막고 어디서 온 규칙인지 표로 보여 주고, 끌 것을 고르게 한 뒤 `disabled_rules` 한 줄만 고친다. 쓰기 전에 바뀌는 줄을 보여 주고 확인을 받는다. 기본값은 여전히 전부 켜짐이다. 커맨드는 여덟이 됐다.

### 수정
- 게이트 상세 문서의 근거 표에 R2a와 R5의 출처가 빠져 있었다. 규칙마다 출처를 밝힌다고 적어 두고 두 규칙은 표 어디에도 없었다. R2a는 Reduce hallucinations의 "Allow Claude to say I don't know"에, R5는 훅 문서의 `PostToolUseFailure` 이벤트에 기댄다. PR 본문 검사(`done.pr`)도 Best practices 행에 넣었다.
- **`/grounded:config`가 표에서 출처 열을 빼먹었다.** 이 커맨드를 둔 이유가 끄기 전에 규칙의 출처를 보여 주는 것인데, 격리한 세션에서 처음 돌려 보니 출처 열 없이 표를 냈다. 스킬에 다섯 열을 못 박고 출처 열을 빼지 말라고 적었다. 고친 뒤 같은 조건에서 출처가 모두 나왔다. 두 번 다 확인 없이 설정 파일을 고치지 않았다.

### 변경
- **CI가 PR마다 두 벌 돌고, Windows 작업은 한 번에 10분을 넘겼다.** 워크플로가 `push`와 `pull_request`에 브랜치 조건 없이 걸려 있어 PR 브랜치에 푸시할 때마다 같은 검사가 두 번 돌았다. 이제 `push`는 main에서만 돌고, 같은 PR에 새로 푸시하면 앞 실행을 취소한다. Windows에서 7~9분 걸리던 fuzz는 PR에서 건너뛰고 main에 올라갈 때 돌린다. 대신 Windows에서만 나는 fuzz 실패는 머지 뒤에야 보인다.

## [1.5.0] - 2026-09-11

### 추가
- **규칙 하나만 끄는 길.** `.grounded.toml`에 `disabled_rules = "R2b"`처럼 적으면 그 규칙만 빠진다. 지금까지 오탐을 만나면 환경변수로 게이트를 통째로 끄거나 플러그인을 끄는 수밖에 없었는데, 환경변수는 한 사람 셸에만 있어 팀이 모른다. **끄기를 쉽게 만드는 변경이 아니라 끄는 행위를 보이게 만드는 변경이다.** 무엇을 껐는지 `events.log`에 `off=[...]`로 남고, 세션마다 저장소 프로필이 실어 준다. 없는 규칙 이름을 적으면 막을 때 그 사실을 알린다. 조용히 안 꺼지는 것이 제일 나쁘다.

### 수정
- **`CONTRIBUTING.md`와 PR 템플릿이 시키는 검사 명령이 없는 경로를 가리키고 있었다.** 훅이 `plugin/` 아래로 옮겨진 뒤에도 `hooks/no-guess-gate/*.sh`를 적고 있어, 그대로 붙여 넣으면 `no matches found`로 죽는다. 기여하려는 사람이 첫 명령에서 넘어지는 상태였다. 새 명령은 전부 실제로 돌려 보고 적었다. CI를 ubuntu와 macos라고 적던 것도 windows를 포함하도록 고쳤다.
- `SECURITY.md`의 서명 확인 예시가 `v1.3.1`이었다.

### 변경
- **커맨드 일곱을 영어로 쓰고, 사용자가 쓴 언어로 답하게 했다.** 게이트가 내보내는 문장은 이미 로케일을 따르는데 커맨드만 한국어였다. 영어권 사용자는 커맨드 목록의 설명부터 읽을 수 없었고, 모델도 한국어 지시를 받아 영어권 사용자에게 한국어로 답할 수 있었다. `SKILL.md`는 로케일로 가를 수 없는 파일이라 영어로 쓰고 본문에 사용자의 언어로 답하라는 지시를 박았다. **한국어 사용자가 받는 답은 달라지지 않는다.**
- `/grounded:status`가 `disabled_rules`와 `fast_test_command`도 함께 보고한다.
- `SECURITY.en.md`와 `CONTRIBUTING.en.md`를 더하고 이슈·PR 템플릿을 두 언어로 바꿨다. 미탐 템플릿에 R5가 빠져 있었다.

## [1.4.1] - 2026-09-11

### 수정
- **경로 형태가 다르면 완료 게이트가 조용히 통과하던 것.** macOS에서 `PostToolUse`는 심링크가 풀린 `/private/var/...`를, `cwd`는 `/var/...`를 준다. 문자열로 비교해 코드 파일을 0개로 세고 그냥 통과했다. 문자열 비교를 먼저 하고, 그것이 0을 셌을 때만 양쪽을 실경로로 풀어 다시 센다. 더하기만 하므로 원래 세던 경우를 건드리지 않는다. 심링크가 낀 경로(`/tmp`·`/var`)에서만 나던 버그지만, **인수 테스트가 임시 폴더에서 돌아 그동안 이 실패를 잡을 수 없었다.**
- **테스트가 훅에 넣는 입력 JSON을 `printf`로 만들도록 고쳤다.** Git Bash는 네이티브 파이썬에 POSIX 경로를 인자로 넘길 때 Windows 경로로 바꾼다. 그래서 `cwd`만 형태가 달라져 Windows CI에서만 완료 게이트 테스트가 빨갰다. 훅이 아니라 테스트 입력의 문제였다.
- `.gitignore`가 `no-guess-gate`의 상태 폴더만 덮고 있었다. `--plugin-dir`로 개발할 때 다른 게이트의 상태가 커밋될 수 있었다.

### 추가
- 상세 문서에 **"얼마나 느려지나"**. 훅별 실측과 턴당 바닥(약 326ms), 끄는 법을 적었다. 게이트가 무엇을 막는지는 315건으로 덮었으면서 **설치하면 얼마나 느려지는지는 재 본 적이 없었다.** 채택을 좌우하는 숫자이고 동종 어느 곳도 공개하지 않는다.

## [1.4.0] - 2026-09-11

### 수정
- 따옴표가 든 파일명 둘이 커밋돼 **Windows CI가 체크아웃 단계에서 죽던 것.** 테스트 생성 중 이스케이프가 깨져 만들어졌다. `tests/invariants.sh`가 그런 이름이 들어오는지 매번 본다.

### 추가
- **`plugin/README.md`.** 설치본에 README가 없었다. 공식 배포 체크리스트에 있는 항목이고 `tdd-guard`도 넣는다. 게이트 넷이 각각 무엇을 막고 어떻게 꺼지는지, 커맨드 일곱, 처음 할 일을 한국어와 영어로 적었다. 설치한 사람이 저장소로 가지 않고도 알 수 있어야 한다.

### 수정
- **프로젝트 가드가 플래그 이름의 언급을 사용으로 읽던 것.** README 표에 무엇을 막는지 적었더니 그 글자 때문에 막혔다. 커밋 메시지 안에 적는 것도 막혔다. 이제 문장을 나눈 뒤 `shlex` 토큰으로 본다. 별도 토큰으로 붙었을 때만 막는다. 근거 게이트에는 있던 "인용은 사용이 아니다"가 여기엔 없었다.

## [1.3.2] - 2026-09-11

### 추가
- `SECURITY.md`에 **설치할 것을 직접 확인하는 법.** 릴리스 태그가 서명돼 있는데 어디에도 안 적혀 있었다. `git tag -v`로 검증하고, `plugin/` 23개 파일과 `hooks.json`을 직접 읽는 절차를 적었다. 서명이 있어도 알리지 않으면 없는 것과 같다.
- `main` 브랜치에 강제 푸시·삭제 금지. 사람들이 내려받는 코드의 히스토리가 조용히 바뀌지 않는다. 리뷰 강제나 필수 체크는 걸지 않았다.

### 수정
- `tests/invariants.sh`의 개수 검사가 `find`로 세다가 CI에서 깨졌다. 앞 단계 `py_compile`이 만든 `__pycache__`까지 세어 24개가 나왔다. **내가 방금 고친 것과 같은 종류의 버그를 새 검사가 다시 냈다.** 이제 `git ls-files`로 센다. 실리는 것은 커밋된 것이다.
- 문서가 "실리는 것은 24개"라고 적었는데 실제는 23개다. 설치본의 24번째는 Claude Code가 만드는 `.in_use/` 표식이다. 사용자에게 확인하라고 안내한 숫자라 `tests/invariants.sh`가 매번 대조한다.

## [1.3.1] - 2026-09-11

### 추가
- `tests/acceptance.sh`. 새 사용자 관점의 인수 테스트다. 빈 npm 프로젝트를 만들어 플러그인을 걸고 게이트와 스킬을 실제로 돌린다. **스킬 일곱은 지금까지 정의만 검사했지 한 번도 실행해 본 적이 없었다.**

### 수정
- **삭제 차단이 명령 전체를 훑어 엉뚱한 짝을 짓던 것.** 삭제 명령이 어딘가에 있고 다른 문장에 테스트 경로가 있으면 삭제로 읽었다. 임시 폴더를 치우는 명령이 테스트 삭제로 막혔다. **이 저장소 작업 중 여섯 번 걸렸다.** 이제 `;`·`&&`·`||`·`|`·개행으로 문장을 나눈 뒤 삭제로 시작하는 문장의 인자만 본다. 테스트 무결성 게이트와 프로젝트 가드 양쪽에 적용했다.
- 빌드 산출물(`__pycache__`·`node_modules`·`.pytest_cache`·`.pyc` 등)을 테스트 파일로 세지 않는다.

## [1.3.0] - 2026-09-11

### 변경
- **플러그인을 `plugin/` 하위 폴더로 옮겼다.** 설치는 폴더를 통째로 복사하고 제외 방법이 없어서, 지금까지 사용자가 CI 워크플로·1,500줄 검증 기록·기여 문서·테스트 스크립트까지 함께 내려받았다. **62개 500K → 23개 124K.** 도는 것은 훅 열과 스킬 일곱뿐이다. 공식 `anthropics/claude-code`(13개 전부)와 `nizos/tdd-guard`가 같은 방식이라 실측으로 확인하고 따랐다.
- 테스트와 도구를 `tests/` 로 옮겼다. `plugin/` 안에 두면 사용자에게 실린다.

### 수정
- **빌드 산출물 삭제가 테스트 파일 삭제로 막히던 오탐.** `tests/__pycache__` 를 지우려다 세 번 걸렸다. `__pycache__`·`node_modules`·`.pytest_cache`·`.pyc` 같은 산출물은 테스트 파일이 아니다. 진짜 테스트 파일 삭제는 그대로 막는다.

### 추가
- README의 "끄기와 제거"에 공식 탈출구 둘. `"disableAllHooks": true`와 `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`이다. 게이트를 끄는 방법을 숨기지 않는다.
- 상세 문서에 **내장 `type: "prompt"` 훅을 쓰지 않는 이유.** 실측하니 프롬프트 훅은 모든 턴에 +1.6초, 우리 판정기는 차단의 4%에서만 8초다. 동작하지 않아서가 아니라 호출 빈도가 달라서다.

## [1.2.0] - 2026-09-10

### 추가
- README에 **알려진 미탐** 절. 막지 못하는 것 넷을 왜 그대로 두는지와 함께 적었다. 오탐만 적고 미탐을 숨기면 그 자체가 근거 없는 주장이다.

### 추가
- **테스트 러너 설정으로 테스트를 빼는 것을 막는다.** `jest.config.*`·`vitest.config.*`·`pytest.ini`·`pyproject.toml`·`.mocharc.*` 등에서 제외 지시어(`testPathIgnorePatterns`, `--ignore=`, `exclude`, `norecursedirs`, …)가 늘면 차단한다. 테스트 파일을 건드리지 않고 설정만 고치면 지금까지 그냥 통과했다. EvilGenie(arXiv 2511.21654)가 "Modified Testing Procedures"로 분류한 행동이고, 그 논문이 꼽은 세 가지 중 우리가 안 막던 것이다. 제외를 줄이거나 무관한 편집은 통과한다.

### 추가
- **규칙 R5: 마지막으로 돌린 명령이 실패했는데 통과·검증을 주장하면 막는다.** R3은 Bash 실행이 0건일 때만 걸려서, **명령을 돌렸는데 실패한 경우가 비어 있었다.** `npm test`가 깨진 뒤 "전부 통과했습니다"가 그대로 나갔다. 공식 훅 문서의 `PostToolUseFailure` 이벤트를 써서 이 턴의 Bash 결과를 `S`/`F`로 남기고, 마지막이 `F`면 성공 주장을 막는다. 실패한 뒤 고쳐서 다시 성공했으면 걸리지 않는다.
- `hooks/no-guess-gate/bashres.sh`. 도구 호출마다 도는 자리라 파라미터 확장만 쓰고 파이썬을 부르지 않는다.

### 수정
- `hooks/lib/unit.sh`의 키 대조 정규식이 `[a-z][a-z.]*`라 **숫자가 든 키를 한 번도 세지 않았다.** `ngg.r0`부터 `ngg.r5`까지 여덟 개가 두 언어 대조에서 빠져 있었다. 46개를 세던 것이 54개가 됐다.

### 수정
- **`selftest.sh`의 `xx-deadlock` 케이스가 어떤 경우에도 실패를 낼 수 없었다.** 교착을 잡으라고 둔 canary인데, 판정이 `실측=BLOCK`까지 요구해서 **세션이 답을 하나도 못 낸 진짜 교착을 `PASS`로 읽었다**(결과 `'None'`, 9턴, 판정 기록 없음). 교착의 정의를 "턴이 깨끗하게 닫히지 않았다"로 바로잡았다.

## [1.1.4] - 2026-09-10

### 추가
- `hooks/no-guess-gate/judge-accuracy.sh`. 판정기 정확도와 소요 시간을 재현 가능하게 잰다. 손으로 재던 것이라 다시 잴 수 없었다. 재측정 결과 정확도 12/12 유지, 중앙값 8초, 최대 9초(이전 12.0초).

### 수정
- **A/B 하네스가 `msg.sh`를 임시 폴더로 옮기지 않았다.** 게이트는 `exit 2`로 막지만 모델이 받는 문장이 `ngg.r0` 같은 키 이름이라 무엇을 하라는지 알 수 없었다. 이 상태로 잰 값이 "게이트를 켜면 더 나빠진다"고 나왔다. 고친 뒤 n=24에서 **도구 0회 답변 75% → 42%, 답당 도구 0.3 → 1.0회**다. README와 `docs/demo.svg`를 이 값으로 맞췄다.
- 카탈로그가 없을 때 `M`을 `msg()` 안에서만 비워 **모든 줄이 첫 키 이름으로** 나오던 것. `tn()`이 직접 비운다.
- `selftest.sh`가 케이스를 디렉터리 수로 세어 12를 13으로 보고하던 것. `run()`이 만드는 `lib/`가 섞여 있었다.
- `judge-accuracy.sh`가 `printf '%.52s'`로 한글을 바이트 단위로 자르던 것.

## [1.1.3] - 2026-09-10

### 수정
- `hooks/invariants.sh`가 `__pycache__`를 훅 모듈로 세던 오탐. CI의 문법 검사 단계가 `py_compile`로 그 폴더를 만들고, 다음 단계가 `hooks/` 아래 디렉터리를 그냥 세다가 세 OS 전부에서 실패했다. 훅 모듈을 "`.sh`를 가진 디렉터리"로 정의를 좁혔다. v1.1.2 태그가 가리키는 커밋이 CI 빨간 상태라 이 판을 낸다.

## [1.1.2] - 2026-09-10

### 추가
- **마켓플레이스 항목을 공식 스키마대로 채웠다.** `$schema`·`version`·`author`·`homepage`·`repository`·`license`·`category`·`tags`·`keywords`·`strict`. `tags`는 Claude Code 안에서의 검색 필드인데 비어 있었다. 플러그인의 실제 유통 경로가 저장소가 아니라 이 목록이다.
- `hooks/invariants.sh` 13건. 저장소가 스스로 지켜야 하는 불변식을 본다. 두 매니페스트와 `CHANGELOG`의 버전·이름, 문서가 적어 둔 커맨드·훅 모듈·퍼징 횟수, 그리고 셸 인용이다. 전부 조용히 어긋나는 것들이라 눈으로 볼 수 없다.
- `hooks/lint-expand.py`. 중괄호 없는 변수 뒤에 한글이 바로 붙은 곳을 찾는다. `"$n개"`는 bash가 `n개`를 변수 이름으로 읽고 `set -u` 아래서는 그 자리에서 죽는다. **이 저장소에서 세 번 났고**, 이번에도 실패 분기에 숨어 있어 통과할 때는 안 보였다.

### 변경
- README의 "비슷한 도구"를 이 공간의 실제 지도로 다시 썼다. cc-safety-net · Probity · failproofai · Stop That Shit을 각자의 설명문 그대로 넣고, 대부분이 `PreToolUse`에서 도구 호출을 막는 데 반해 이쪽은 `Stop`에서 턴을 막는다는 차이를 표로 밝혔다. 이웃 셋이 빠져 있었다.

### 수정
- CI의 `shellcheck`·`bash -n` 글롭이 `hooks/*/*.sh`뿐이라 `hooks/` 바로 아래 파일이 빠졌다. **`fuzz.sh`가 한 번도 린트되지 않았다.** `hooks/*.sh`를 더했고, 드러난 지적을 전부 고쳤다.
- 문서가 퍼징을 "23종 × 훅 아홉"이라 적었는데 실제는 입력 24종(파일 23 + 카나리 1) × 훅 아홉 = 216회다. 산수가 맞지 않았다. 이제 `invariants.sh`가 문서와 실제를 대조한다.
- `hooks/invariants.sh`의 실패 안내 두 곳이 `$n_sk개` 꼴이라 그 분기에 닿는 순간 죽었다. 검사가 실패를 알리지 못하고 죽는 것이 가장 나쁘다.

## [1.1.1] - 2026-09-10

### 추가
- **Windows를 CI에서 실제로 검사한다.** `windows-latest`를 매트릭스에 넣었다. README가 세 곳에서 Windows를 주장했는데 한 번도 돌려 본 적이 없었고, 넣어 보니 13건이 깨졌다.
- `hooks/attack-surface.sh`. `SECURITY.md`가 글로 약속한 것을 검사로 바꾼다. 네트워크 명령 없음, 모델 호출은 `judge.py` 하나, 판정기 격리 플래그 셋, 프로필이 `.env` 값을 안 읽음, 시스템 경로에 안 씀, 훅 열 개 전부 타임아웃.
- `.github/workflows/release.yml`. 태그를 밀면 태그와 `plugin.json` 버전이 같은지, `CHANGELOG`에 그 항목이 있는지 보고 전체 검사를 돌린 뒤 릴리스를 만든다.
- CI에 `actionlint`. 액션을 커밋 SHA로 고정해 두는 저장소라 배선이 틀어지면 공급망이 흔들린다.

### 수정
- **Windows에서 한국어가 파이썬을 지나면 훅이 죽던 것.** Windows 파이썬은 stdio와 파일 기본 인코딩이 UTF-8이 아니라 레거시 코드페이지(`cp1252`)다. `common.sh`의 `py()`가 우리 호출에만 `PYTHONUTF8=1`을 붙인다. 전역으로 걸지 않는 이유는 `done-gate`가 남의 테스트 명령을 `eval`로 돌리기 때문이다.
- **판정기가 Windows에서 `cmd.exe`로 떨어지던 것.** `subprocess.run(cmd, shell=True)`는 Windows에서 `COMSPEC` 뒤에 `cmd.exe` 문법인 `/c`를 붙인다. `executable`로 bash를 넣어도 그 `/c`가 남는다. 셸을 `[bash, -c, cmd]` argv로 직접 부른다.
- 판정기가 자식 출력을 읽을 때 `encoding`을 주지 않아 레거시 코드페이지로 읽던 것. `encoding="utf-8", errors="replace"`로 못 박았다. 플랫폼과 무관한 결함이다.
- `attack-surface.sh`의 시스템 경로 검사가 ERE에 없는 전방탐색(`(?!…)`)을 써서 `grep`이 죽고 늘 통과하던 것. 변별 시험에서 잡았다.

## [1.1.0] - 2026-09-10

### 추가
- **게이트 메시지가 한국어와 영어 두 벌이다.** 사람과 모델에게 나가는 문장 46개를 `hooks/lib/msg.sh` 한 곳으로 옮겼다. `NGG_LANG=ko|en`이 우선하고, 없으면 `LC_ALL` → `LC_MESSAGES` → `LANG`을 보고 `ko` 계열일 때만 한국어다. 규칙 판정은 언어와 상관없이 같다.
- `hooks/lib/unit.sh` 17건. 두 언어의 키가 맞는지, 언어 결정 순서가 맞는지, 없는 키가 조용히 사라지지 않는지 본다.
- 행동 강령(Contributor Covenant 2.1). 신고 창구는 저장소의 비공개 신고이고, 실제로 켜 두었다.
- `.github/dependabot.yml`. 워크플로의 액션을 커밋 SHA로 고정해 두어 주마다 올려 준다.
- 이슈 템플릿 `config.yml`. 게이트 상세 문서와 비공개 신고 창구로 먼저 보낸다.

### 변경
- `events.log`의 면제 표식을 언어와 무관한 ASCII로 바꿨다(`(불가 면제)` → `(exempt:cannot)`). README가 이 줄을 이슈에 붙이라고 안내하는데, 한국어를 모르는 사람이 자기 로그를 읽을 수 없었다.

### 수정
- **`LC_ALL=C`처럼 UTF-8이 아닌 로케일에서 R1이 조용히 빠지던 것.** 문장 분리기가 `[.!?。]`를 썼는데 대괄호 안의 `。`가 세 바이트로 흩어져 한국어 글자를 한가운데서 잘랐다. 교체(`|`)로 바꿨다. 이번 변경 이전부터 있던 버그이고, 여섯 로케일에서 전 스위트 통과를 확인했다.
- README 두 개가 옮겨 적은 차단 메시지가 실제 출력과 달랐다. 실행해서 받은 문자열의 접두가 되도록 고치고 줄인 자리는 `(…)`로 밝혔다. `docs/demo.svg`도 같은 기준으로 다시 만들었다. 그 시나리오의 실제 판정은 `[R1]`이 아니라 `[R0 R1]`이다.

## [1.0.0] - 2026-09-10

### 추가
- 문서를 두 층으로 나눴다. `README`는 무엇이 막히는지와 설치까지만 두고, 게이트 넷의 동작·커맨드 일곱·근거 문서는 `docs/gates.ko.md`(영문 `docs/gates.en.md`)로 옮겼다.
- 차단부터 실측, 다시 답하기까지를 담은 `docs/demo.svg`. 스크립트도 외부 자원도 없는 정적 이미지다.
- `.gitattributes`로 줄바꿈을 LF로 고정한다. 훅이 셸 스크립트라 CRLF로 체크아웃되면 실행이 깨진다.
- **커맨드 일곱**: `/grounded:spec` · `init` · `tdd` · `ship` · `handoff` · `status` · `auto`. 전부 사용자 전용(`disable-model-invocation: true`)이다. 계획·탐색·리뷰·실행 확인·반복은 내장(plan mode, Explore, `/code-review`, `/verify`, `/goal`)을 부르고 새로 만들지 않았다.
- **저장소 프로필**: `SessionStart`에서 패키지 매니저·스택·검사 명령·append-only 경로·게이트 상태를 스무 줄 안팎으로 컨텍스트에 싣는다. 모델을 부르지 않고 비밀 파일 값은 읽지 않는다. `NGG_PROFILE=0`으로 끈다.
- **테스트 무결성 게이트**: 테스트 파일에 무력화 표기(`.skip`, `.only`, `xit`, `@pytest.mark.skip`, `#[ignore]`, `t.Skip` 등)가 늘거나 단언이 줄면 그 편집을 막는다. 테스트 파일 삭제 명령도 막는다. 기댓값 수정과 단언 추가는 통과한다. `NGG_TESTGUARD=0`으로 끈다.
- **프로젝트 가드**: `.grounded.toml`의 `append_only` 경로에서 기존 파일의 수정·삭제를 막는다. 새 파일 추가는 허용한다. `git commit --no-verify`는 설정과 무관하게 막는다. `NGG_GUARD=0`으로 끈다.
- **완료 게이트**: 코드 파일을 고친 턴은 저장소의 검사가 통과해야 끝난다. 검사 명령은 `.grounded.toml` → `package.json` → `Makefile` → `pyproject.toml` 순으로 찾는다. 못 찾으면 알리고 막지 않고, 시간을 넘겨도 알리고 막지 않는다. `NGG_DONE=0`으로 끈다.
- 근거 게이트: 도구 실행 없이 로컬 상태를 단정하거나(R0·R1), 확인을 미루거나(R2a), 추정으로 메우거나(R2b), 검증했다고 주장하거나(R3), 막힌 뒤 실측 없이 다시 끝내려는(R4) 턴을 막는다.
- 면제 셋: 질문(되묻는 답), 불가(실측이 불가능한 이유를 밝힌 답), JSON(답 전체가 JSON).
- 의미 판정기 `judge.py`: R2a·R2b만 걸렸을 때 격리한 Haiku에게 의견인지 상태 주장인지 묻고 의견이면 풀어 준다. 풀어 줄 수만 있고 새로 막지 못한다. `NGG_JUDGE=0`으로 끈다.
- 플러그인 배선 `hooks/hooks.json`, 상태는 `${CLAUDE_PLUGIN_DATA}`.
- 결정적 단위 테스트 `unit.sh`, 실제 프롬프트 회귀 `selftest.sh`, 검증 기록 `docs/VERIFICATION.md`.

### 변경
- 판정 모델을 `NGG_JUDGE_MODEL`로 고를 수 있다. 기본은 `haiku`이고 이전에는 박혀 있었다. 공식 `/goal`도 평가 모델을 바꿀 수 있게 해 둔다.
- 판정기의 결과와 소요 시간을 `events.log`에 남긴다(`judge=released|kept|failed`). 이전에는 풀어 준 경우만 보여 판정이 도는지 셀 수 없었다.
- 차단 메시지가 **앞 답이 화면에 남아 있다**는 사실과 통째로 다시 쓰지 말라는 지시를 담는다. 이 안내가 없어 같은 답이 두 번 나가던 일이 실측 14건 있었다.
- 완료 게이트가 턴마다 전체 스위트를 돌리지 않는다. `fast_test_command`을 적으면 턴 끝에는 그것만, 전체는 커밋 직전에 한 번.
- `git commit --no-verify` 차단은 건너뛸 커밋 훅이 실제로 있는 저장소에서만 동작한다.
- R2a를 도구를 한 번도 안 쓴 경우로 좁혔다. 공식 Reduce hallucinations의 "Allow Claude to say I don't know"와 맞춘다.
- 의견에 붙은 유보("나아 보인다")와 인용(따옴표·백틱·공백 없는 괄호 목록)은 R2b 대상이 아니다.

### 수정
- python3가 없으면 조용히 통과하던 것을 비차단 오류로 드러낸다.
- 상태 파일이 없을 때 stderr에 새던 리디렉션 잡음을 막았다.
